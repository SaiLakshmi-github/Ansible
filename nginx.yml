- name: Install and run nginx
  hosts: web
  become: yes
  tasks:
  - name: Install nginx
    ansible.builtin.package:
      name: nginx
      state: latest
  - name: Start service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
